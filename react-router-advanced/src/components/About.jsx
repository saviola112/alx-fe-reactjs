import React from 'react';

const About = () => {
  return (
    <div>
      <h2>About Our Application</h2>
      <p>This application demonstrates advanced routing techniques including protected routes, nested routes, and dynamic routing using React Router DOM.</p>
    </div>
  );
};

// CRITICAL FIX: The component definition and default export are essential
export default About;